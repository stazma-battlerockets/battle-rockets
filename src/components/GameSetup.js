import BattleRocketGrid from "./BattleRocketGrid";

const GameSetup = ({player}) => {

    
    return (
        <div>
            
            <BattleRocketGrid />
        </div>

    );
};

export default GameSetup;